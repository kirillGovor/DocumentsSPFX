export interface ISite {
  text: string;
  key: string;
}

export interface ISiteProperty {
  name: string;
  key: string;
}

export interface IDocument {
  ContentType: string;
  LinkFilename: string;
  id: string;
  webUrl: string;
}
